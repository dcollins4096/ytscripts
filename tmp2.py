

import pfdiff
reload(pfdiff)
#ud = pfdiff.udiff(dir1,dir2,frames=[0],fields=['Density'])

Slice1, Slice2 = FindOverlap.FindOverlap(self.uber1,self.uber2,n1,n2,grid1,grid2,field1,shift,nGhost,self.p['grid_direct'])
